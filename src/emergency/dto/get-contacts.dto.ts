// Get Contacts DTO - Emergency contacts lene ke liye (empty DTO kyunki saare contacts chahiye)
export class GetContactsDto {
  // Koi fields nahi chahiye kyunki saare emergency contacts fetch honge
  // Contacts static hain aur database se aate hain
}
